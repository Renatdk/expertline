<%= form_for(@subcatalog) do |f| %>
  <% if @subcatalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcatalog.errors.count, "error") %> prohibited this subcatalog from being saved:</h2>

      <ul>
      <% @subcatalog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
